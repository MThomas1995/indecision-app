// React Class based component (Requires one method to work 'render()')

// Stateless component (No render() required) = Presentational function
// No access to "this" and no access to state
// Faster than state components
// For non lifectyle methods

// Default Props
Header.defaultProps = {
    title: 'Indecision'
}